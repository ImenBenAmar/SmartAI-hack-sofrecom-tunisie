// PostCSS is currently not used since we're styling with Boosted (Bootstrap).
// Keep this file minimal to avoid Tailwind-specific processing.
const config = {
  plugins: [],
};

export default config;
